// app/index.tsx
import SplashScreen from "./splash"; // Import the SplashScreen

export default function Index() {
  // Here, we simply render the SplashScreen as the first entry point
  return <SplashScreen />;
}
